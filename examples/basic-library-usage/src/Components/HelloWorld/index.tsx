import React from "react";
import { SizeFontBase } from "@cpqd-quati-tokens/web/ts/tokens";

const HelloWorld = () => {
  return (
    <div>
      Hello World From Library Component that uses quati-tokens, like:
      SizeFontBase {SizeFontBase}
    </div>
  );
};

export default HelloWorld;
